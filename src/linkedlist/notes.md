Arrays:
    Are of fixed size and their storage location is continuous
    int arr[4] = [1,2,4,5]
    u cant add more elements

Linked List:
    dynamic size and storage location are random
    Usage:
        Stacks
        Queues
        Example browser navigation
    Space for this example:

        Node(int n, pointer)
        for a 32bit pc the space required is 8 bytes(4 bytes of data(**if integer**) and 4 bytes of pointer)
        for a 64bit pc the space required is 12 bytes(4 bytes of data(**if integer**) and 8 bytes of pointer)